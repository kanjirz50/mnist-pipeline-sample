# mnist-luigi-gokart-sample

Simple machine learning pipeline examples on Luigi or gokart.

Each example shows a simple machine learning pipeline that includes data fetching, preprocessing, training and evaluation for MNIST dataset.